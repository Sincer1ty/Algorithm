class Solution:
    def findEvenNumbers(self, digits: List[int]) -> List[int]:
        n = len(digits)
    
        arr = []
        for i in rangen(n):
            for j in range(i, n):
                

        
        